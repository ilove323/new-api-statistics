name: CI
on:
  push:
    branches: [main]
  pull_request:
  workflow_call:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  tests:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    strategy:
      matrix:
        python: ["3.12", "3.13", "3.14"]
        postgres: ["15", "16"]
    services:
      postgres:
        image: postgres:${{ matrix.postgres }}
        env:
          POSTGRES_USER: ci
          POSTGRES_PASSWORD: ci-test-only
          POSTGRES_DB: statistics_test
        ports: ["5432:5432"]
        options: >-
          --health-cmd "pg_isready -U ci -d statistics_test"
          --health-interval 5s --health-timeout 5s --health-retries 10
    env:
      PGHOST: localhost
      PGPORT: "5432"
      PGUSER: ci
      PGPASSWORD: ci-test-only
      PGDATABASE: statistics_test
      MONITOR_DATABASE_URL: postgresql://ci:ci-test-only@localhost:5432/statistics_test
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5
        with:
          python-version: ${{ matrix.python }}
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: "22"
      - run: pip install . ruff build
      - run: ruff check src tests
      - run: ruff format --check src tests
      - run: python -m unittest discover -s tests
      - run: python tests/check_balance_database.py
      - run: python tests/check_migrations.py
      - run: for file in src/new_api_statistics/static/*.js; do node --check "$file"; done
      - run: python -m build

  container:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
      - run: docker build -t statistics-ci .
      - name: Verify runtime and build boundaries
        run: |
          docker run --rm --entrypoint python statistics-ci -c "from pathlib import Path; assert not Path('/app/.git').exists(); assert not Path('/app/.env').exists(); assert not Path('/app/DEPLOYMENT.private.md').exists()"
          docker run -d --name statistics-ci -p 127.0.0.1:8091:8000 statistics-ci
          for attempt in $(seq 1 30); do
            if curl -fsS http://127.0.0.1:8091/healthz; then break; fi
            sleep 1
          done
          curl -fsS http://127.0.0.1:8091/healthz
          test "$(curl -s -o /dev/null -w '%{http_code}' http://127.0.0.1:8091/statistics/)" = 401
      - if: always()
        run: docker logs statistics-ci || true

  secrets:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          fetch-depth: 0
      - name: Scan Git history for secrets
        run: docker run --rm -v "$PWD:/repo" zricethezav/gitleaks:v8.24.3 git /repo --redact --no-banner
